import { View, Text } from "react-native";
import React from "react";

const MediaOptions = () => {
  return (
    <View>
      <Text>MediaOptions</Text>
    </View>
  );
};

export default MediaOptions;
